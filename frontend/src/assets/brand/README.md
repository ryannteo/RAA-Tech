# Brand assets

Drop the real Ryde brand kit here once we have it: logo (svg/png), exact
hex colors, approved fonts.

Until then:
- `tailwind.config.js` defines a placeholder `ryde` color (pink/red, based on
  the wordmark in the hackathon brief).
- `Header.tsx` renders a text-based placeholder wordmark.

Swap both once real assets land.
